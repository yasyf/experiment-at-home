{"v":1,"lamport":21,"ops":[{"kind":"add_comment","body":"Round-7 lifecycle audit on 26d64def: T1–T4 verified SOLID at source (T1 gating traced into both downstream cost assignments, no double-count — a wall-deadline-during-recovery race is a double CALL never a double COUNT; T4's [\"usd\"] sweep: exactly three indexers, all behind validate_journal; nightly/cli never touch it). THREE live gaps → A3.9 (round 8): U1 HIGH outer cancellation (first SIGINT → plain CancelledError under bare anyio.run; caught by neither except-Exception nor the own-deadline wall branch) loses billed cost with no trace — found independently by verifier AND codex; also bites post-proposal_completed; launchd SIGTERM = the unattended case; fix = shielded accounting cleanup on anyio.get_cancelled_exc_class(), always re-raise. U2 HIGH journal.append failure (loop.py:611, outside the single latch wrapper) vaporizes a fully-computed PAID outcome — OSError → shielded latch → accounting abort. U3 HIGH sidecar torn FINAL line fails open (T3's class on the sidecar side) — fail closed on final, malformed-skip stays for non-final. Accepted as documented: the round-5 latch residue (unchanged) + NEW fsync residue (appends process-crash resistant, not power-loss durable — design note, one docstring line). Test adequacy: 9/12 new cases genuinely fail on parent, 3 legitimate negative controls. RELEASE CRITERION PRE-DECLARED for round 8: HIGH live-reachable findings block; exotic-input LOW/MED triage to post-release backlog — the loop is bounded, not open-ended."}]}