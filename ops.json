{"v":1,"lamport":8,"ops":[{"kind":"add_comment","body":"A3.3 landed: c8b8a678 on e5f3afff (athome-a3/codex sol xhigh; 934 passed +6 targeted tests; my independent suite run green; ty unchanged — 16 research/ diagnostics vs 19 at the A3.1 baseline, none new). F1: ClaudeCodeDriver holds a ProposalRecovery record and exposes async recover_cost() (shielded envelope read); the Driver Protocol gains recover_cost; run_unit tracks proposal_started and on wall-cancel shield-recovers a billed-but-unreturned proposal's cost to a kind=wall_cancel sidecar event — tested with the REAL driver and a fake_claude printing a $0.60 envelope then sleeping through the poll (the genuine race), asserting recovery + no journal row + resume trips BudgetExhausted. F2: infra_cost requires isfinite AND \u003e=0 (bool exclusion kept); journal boundary confirmed structurally protected (envelope_cost validates finite\u003e=0, validate_journal guards resume) — no redundant validation added. F4: record_infra_event gains kind Literal[retry,wall_cancel]; infra_retries counts only retry (legacy no-kind → retry); costs still sum across kinds. F3: documented in the failures.py module docstring (I/O-failed wall-cancel flush → manual ledger check on resume), not guarded. Ops note: a concurrent venv re-sync without the dev extra transiently broke pytest spawn (known shared-env friction) — uv sync --extra dev restored it. Release gate: re-verify refuter running on c8b8a678 (attack the recovery path's own races, the non-finite matrix, legacy sidecar tolerance, regression sweep); clean pass → push + release."}]}