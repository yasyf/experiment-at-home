{"v":1,"lamport":14,"ops":[{"kind":"add_comment","body":"A3.5 complete (squashed, supersedes 07ebc730): 002c5613 on bba8159c — 959 passed +14 total new tests across the round; ty/ruff unchanged. H1: validate_driver_cost at the THREE earliest cost-entry points (post-propose, timeout-recovered, wall-cancel-recovered); the A3.4 journal-append check removed (one choke-point). H2: recovered_cost = envelope_cost — the assume-zero branch deleted for BOTH callers per the full-inversion decision; the two hung-agent timeout tests now assert AccountingIntegrityError (deliberate behavior change). M1: shielded cost-free accounting_abort breadcrumb on any AccountingIntegrityError via a run_unit wrapper; run() startup refuses while unreconciled; the explicit re-bill regression proves prior sidecar spend still counts after marker removal (unit re-runs under REMAINING budget, not full cap). M2: Driver.preflight() hook (no-op default) — ClaudeCodeDriver checks claude --version against CLAUDE_CLI_ENVELOPE_FLOOR=(1,0,22) → PreflightFailure pre-spend; PreflightFailure moved to errors.py (preflight re-exports); no legacy total_cost parsing. M3: failures.accounting_aborts() → MorningReport → nightly.report() → CLI summary JSON. Round-4 refuter dispatched on 002c5613 (attack surface: choke totality, the recover_cost None boundary, version-parse edges incl. non-semver → must not silent-pass, re-export import risks, M3 malformed-row exclusion, full regression sweep, per-test mutation adequacy). Clean verdict = push + release."}]}