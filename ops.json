{"v":1,"lamport":22,"ops":[{"kind":"add_comment","body":"A3.9 landed: 13d90e5f on 26d64def (1006 passed; ty zero diagnostics on changed files). U1: shielded except anyio.get_cancelled_exc_class() with own-deadline deferral to the wall branch; match on (proposal_started, proposal_completed): in-flight → recover_cost + wall_cancel flush or latch, completed → flush known cost, pre-spawn → nothing; ALWAYS re-raises. U2: journal.append OSError → shielded latch + AccountingIntegrityError; restart-never-reproposes pinned. U3: sidecar torn FINAL line → AccountingIntegrityError via infra_events (all three consumers inherit); non-final malformed-skip unchanged — faithful T3 mirror. fsync residue documented in the accepted-limitations block. Round-8 re-refute dispatched on 13d90e5f — THE DECISION ROUND under the pre-declared criterion (HIGH live-reachable blocks → A3.10; exotic LOW/MED → explicit post-release backlog list; closed lifecycle table + nothing live-reachable = release-ready → I push and ship). Probes: cancellation during the shielded cleanup itself, outer-cancel/deadline race, double-flush vs the wall branch, cancellation-semantics preservation, non-OSError append failures, U3 final-line valid-JSON-wrong-schema consistency with G4, lifecycle table completeness."}]}