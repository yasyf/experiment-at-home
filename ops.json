{"v":1,"lamport":16,"ops":[{"kind":"add_comment","body":"A3.6 landed: d45d4d09 on 002c5613 (976 passed, +17 tests; ty/ruff unchanged). R1: recovered_cost wraps the log read — OSError/UnicodeDecodeError → CostError → accounting abort on both callers, flowing through the run_unit wrapper (latch + marker). R2: finite_number/envelope_cost overflow-safe (OverflowError from float()/isfinite on giant ints → abort; finite_number's other consumers validate_journal/decide inherit). R3: atomic per-run latch file \u003cname\u003e.abort.json via tmp+fsync+os.replace, written BEFORE the best-effort sidecar breadcrumb; startup refuses on FILE existence (sidecar stays for M3 counting); latch-write failure logs and the abort still raises (documented F3 residue). R4: claude_cli_version fullmatches an anchored 'x.y.z (Claude Code)' shape — wrapper/Node/runtime/prerelease/garbage/exec-error all PreflightFailure, fail closed. R5: two consecutive refused restarts in the regression. Round-5 refuter dispatched on d45d4d09 with the tree-verification control; the sharpest new probe: verify the anchored R4 regex against the REAL current claude --version output (a mismatch = self-DoS on every healthy preflight = HIGH). Clean verdict ships the release."}]}