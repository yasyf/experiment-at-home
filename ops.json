{"v":1,"lamport":18,"ops":[{"kind":"add_comment","body":"A3.7 landed: df1b9460 on d45d4d09 (988 passed +12; ty unchanged — 12 diagnostics on touched files all pre-existing false-positives newly visible via journal.py/spec.py; ruff clean). S1: captured_cost/recovered_cost share read_cost_log() — OSError/UnicodeDecodeError → CostError on BOTH the success and recovery paths. S2: finite_number is a total predicate (subclass __float__ raising anything → False); codex's correct check-back deviation: finite-only in finite_number (metric callers carry valid negatives), \u003e= 0 gate kept in the COST callers. S3: infra_cost whole-file OSError → AccountingIntegrityError; per-row via cycle-free sidecar_cost(); parse_event catches ValueError. S4 sweep: 33 sites classified; sweep-found extras fixed in-commit: journal.py Journal.open read/decode/JSON/shape failures → PoisonedJournal, and PoisonedJournal now SUBCLASSES AccountingIntegrityError (taxonomy unified; raised at run() startup outside run_unit). Latch residue acceptance documented in the module docstring. Round-6 re-refute dispatched on df1b9460: independently re-enumerate the three classes and DIFF against the table (a missed site = finding), spot-check the provably-non-accounting verdicts, probe the PoisonedJournal widening at every except-AccountingIntegrityError site (a poisoned journal must not write a spend latch), quick regression sweep. Clean verdict ships the release."}]}