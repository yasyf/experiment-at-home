{"v":1,"lamport":20,"ops":[{"kind":"add_comment","body":"A3.8 landed: 26d64def on df1b9460 (1000 passed +12; ty/ruff unchanged). T1: recovery placed in execute_unit's except-Exception handler gated on classification!=accounting AND proposal_started AND NOT proposal_completed (driver.py untouched; wall-cancel branch unchanged since cancellation is BaseException) — a post-spawn interruption (launch pidfile OSError, await_exit read faults) now recovers a complete envelope's number into whichever downstream cost record applies, or aborts with the latch; post-propose faults keep the propose cost. T2: record_infra_event append OSError → AccountingIntegrityError (fail-closed; record_accounting_abort stays best-effort per the F3 acceptance). T3: Journal.open json.loads()es the last non-empty raw line → torn/partial final line = PoisonedJournal; progress.py untouched. T4: missing usd key → PoisonedJournal at validate; summations index [\"usd\"] directly now that presence is guaranteed. Round-7 re-refute dispatched on 26d64def: Part A probes the T1 gating (BaseException-not-Exception interruptions, double-recover, both downstream cost assignments), T2 exception coverage, T3 newline/whitespace edges + other progress.py consumers untouched, T4's CRITICAL check that every [\"usd\"] indexer sits behind validate_journal (independent readers would now KeyError raw); Part B is the closing lifecycle audit — every stage spawn→resume must have an explicit, tested failure semantic (recover-exactly-once / abort-with-latch / poisoned / documented-residue) with cites; Part C regression sweep + adequacy. Clean verdict = push + release."}]}