{"v":1,"lamport":24,"ops":[{"kind":"add_comment","body":"A3.10 landed: a93f14f5 on 13d90e5f (1009 passed +3; ty zero diagnostics on changed files). STRUCTURAL: the A3.9 except-cancelled clause is DELETED; execute_unit wraps each attempt in try/finally; per-attempt recorded flag set at all eight durable exits; the shielded epilogue (loop.py:509-524) fires only if not recorded — recover in-flight, record wall_cancel when completed-or-cost\u003e0, catch its own AccountingIntegrityError to write the latch, never raise from the finally (propagating exception unmodified). Both round-8 races now land in the finally with recorded=False. Implementer PROVED adequacy: ran both race tests against the parent in an isolated worktree — both fail there (assert False or False: no event, no latch), pass on the fix. One correct check-back: the no-double-record test is a cross-version guard (normal completion already accounts on the parent), not a parent-failing repro. Round-9 re-refute dispatched: recorded-flag exit-by-exit trace (missed exit = double-record; early set = no-op on an unrecorded path), finally-raise-replaces-propagating-exception audit (no epilogue path may raise), the sharpest question — post-epilogue control flow when the latch path fires under a plain CancelledError: does run() still classify the abort correctly or is the latch alone the surface?, epilogue-shield-under-cancellation, deleted-clause coverage (the post-completed flush), lifecycle final pass. Clean verdict = push + release immediately."}]}