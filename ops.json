{"v":1,"lamport":2,"ops":[{"kind":"add_comment","body":"A-track progress (2026-07-16, cc-steer session): A1 contract memory + frozen baseline committed (356eec34); A2 mandatory preflight probes committed (490832bb, 4 codex rounds, 2 structural contradictions resolved by fable: evaluation passed as BakeoffSpec kwarg mirroring train.run; measure() for reachability overlays; frozen baseline = never-drifting original anchor; resumed digest-mismatch raises). Both sol-reviewed. In-flight fix queue (unpushed stack, one writer at a time): A1.1 (opus) — sanitize contract-rendered history at history_line (control chars, backticks, line cap) + typed MetricShapeError so candidate text never reaches journaled crash descriptions; A2.1 (queued, codex) — read_baseline parse + reachability copytree wrapped into PreflightFailure, empty dataset rejected, known_good_dir validated repo-relative (no absolute/../empty). Next: A3 infra-vs-candidate failure split (opus), A4 BaselineStore, A8 unblockers (9B BASE_MODELS entry + mirror_cc_notes passthrough) → release so cc-steer C2 can pin. Nothing pushed yet; main still at f04a58ca."}]}