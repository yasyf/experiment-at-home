{"v":1,"lamport":6,"ops":[{"kind":"add_comment","body":"A4.1 landed: 6c26f562 on d0f1e5c8 (14 files; codex sol xhigh via athome-a41, diff-read verified + independent full-suite cross-check green, 921 passed/14 skipped). Key shapes: _comparability() hashes task __module__+__qualname__ (never the raw callable — canonical_json's default=str would embed a memory address and break hash stability) + per-arm name/base_url/model (client_factory excluded as connection plumbing) + primary_metric/tiebreak; corpus stays only in dataset_digest. BaselineStore.put = INSERT OR IGNORE + read-back compare → BaselineConflict (raced tests via two store instances + anyio.Event: same-value converges, different-value exactly-one-conflict). train preflight opens BaselineStore at settings.baseline_root with a throwaway get() wrapped into PreflightFailure. Shared mirror_cc_notes_option click decorator on research run + nightly install (launchd tuple conditionally appends --mirror-cc-notes). TinkerPrice for Qwen/Qwen3.5-9B = 0.44/1.33/1.33. No deviations, no push. Sequencing continues: A3.2 dispatched to athome-a3 (task #35 brief: H1 billed-flag double-count, H2 cancellation cost swallow, H3 max_usd enforcement during retry chain, H4 narrow CandidateFault wrap, M tolerant events reader, m Measurement.produced semantics, 3 stale annotations) → targeted verify → release."}]}